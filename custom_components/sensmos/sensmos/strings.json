{
  "config": {
    "step": {
      "user": {
        "title": "Add Sensmos node",
        "description": "Enter your node's address and PIN. The address is shown in the Sensmos app (e.g. 192.168.68.71 or sensmos-ab12cd.local).",
        "data": {
          "host": "Node address (IP or hostname)",
          "pin": "Node PIN"
        }
      }
    },
    "error": {
      "invalid_pin": "Wrong PIN.",
      "cannot_connect": "Cannot reach the node. Check the address and that the node is online."
    },
    "abort": {
      "already_configured": "This node is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Sensmos node",
        "menu_options": {
          "feed_pub": "➕ Feed a native entity (pub.*) from HA",
          "feed_own": "➕ Feed a custom entity (own.*) from HA",
          "feed_remove": "➖ Remove a feed mapping",
          "subscribe": "📡 Subscribe to another node",
          "settings": "⚙️ Settings (node events)"
        }
      },
      "feed_pub": {
        "title": "Pick a native node entity",
        "description": "These are entities the network rewards. Pick one — in the next step you'll choose a matching Home Assistant sensor.",
        "data": { "node_entity": "Native entity (unit)" }
      },
      "feed_pub_entity": {
        "title": "Pick the Home Assistant sensor",
        "description": "Feeding **{node_entity}** (unit: {unit}). The list is filtered to compatible sensors; units are converted automatically (e.g. kW → W).",
        "data": { "ha_entity": "Home Assistant sensor" }
      },
      "feed_own": {
        "title": "Feed a custom entity",
        "description": "Any HA sensor or binary sensor can be stored on the node as an own.* entity for use in node scripts. The unit is taken from the HA entity automatically; binary sensors (e.g. motion) become 1/0.",
        "data": {
          "name": "Name on the node (e.g. boiler → own.boiler)",
          "ha_entity": "Home Assistant sensor or binary sensor"
        }
      },
      "feed_remove": {
        "title": "Remove feed mappings",
        "data": { "remove": "Mappings to remove" }
      },
      "subscribe": {
        "title": "Subscribe to another node",
        "description": "Your node will receive the other node's data (billed daily in GALU). You'll see what's available before confirming.",
        "data": {
          "esp_id": "Target node device ID",
          "prefix": "Local prefix (e.g. sub → sub.*)",
          "days": "Days"
        }
      },
      "subscribe_confirm": {
        "title": "Confirm subscription",
        "description": "Node **{esp_id}** shares {count} entities:\n\n{listing}\n\nData will appear in HA as sensors named **{prefix}.*** within a minute. Duration: {days} days, billed daily in GALU."
      },
      "settings": {
        "title": "Settings",
        "data": {
          "webhook_enabled": "Node events in HA (sensmos_event / sensmos_message)"
        }
      }
    },
    "error": {
      "invalid_name": "Invalid name — use letters, digits, underscore.",
      "already_mapped": "This entity is already mapped.",
      "target_unavailable": "Could not fetch the target node's catalog."
    },
    "abort": {
      "cannot_connect": "Cannot reach the node.",
      "no_native_left": "All native entities are already mapped.",
      "no_feeds": "No feed mappings yet.",
      "subscribe_ok": "✅ Subscribed! New sensors will appear shortly.",
      "subscribe_failed": "Subscription failed.",
      "insufficient_balance": "Not enough GALU for the first day of the subscription."
    }
  }
}
